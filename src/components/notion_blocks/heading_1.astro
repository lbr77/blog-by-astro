---
import BlogRender from "../Content.astro";
let { block,id } = Astro.props;
---

<h1 class="text-4xl font-bold mt-20 mb-8" set:html={block.text} id={id}/>
{block.children_level != 0 ? <BlogRender blocks={block.children} /> : ""}